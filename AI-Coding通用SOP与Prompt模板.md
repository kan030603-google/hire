# AI Coding 做题手册

适用于提供 README、现有源码、公开样例和隐藏评测的 AI Coding 笔试。按下面顺序执行，替换 Prompt 中的 `【占位内容】` 即可。

## 一 做题流程

1. 备份原始源码，创建 4 个独立候选工作区。
2. 两个工作区使用“文档驱动 Rollout”，另外两个使用“纯 Rollout”。
3. 将 4 个版本分别放回提交目录，运行相同的公开样例和隐藏评测。
4. 选择最高分且无明显违规的版本作为 MVP，并保存检查点。
5. Critic 持续设计边界用例、定位问题；Actor 在人工确认后修复。
6. 每轮重新评测：分数不降则保留，出现回归则恢复检查点。

过程只保留三个文档：

- `SPEC.md`：验收合同，说明什么结果才算正确。
- `PLAN.md`：任务拆解、优先级和状态。
- `EVOLUTION.md`：问题、修改、测试结果、分数和演进历史。

`EVOLUTION.md` 同时承担问题清单和验证记录，不再单独创建 `ISSUES.md` 或 `VERIFY.md`。协作文档和 Critic 测试放在 `_ai_work/`，提交时只提交题目要求的源码。

## 二 备份和创建工作区

先在源码目录外新建管理目录，在该目录开启会话并发送：

### Prompt 1 工作区管理脚本

```text
请在当前管理目录创建 workspace_manager.py。题目源码目录是：

【源码绝对路径】

脚本只使用 Python 标准库，提供以下命令：

- init：将原始源码保存到 source_backup，并复制出 branch_doc_01、branch_doc_02、branch_pure_01、branch_pure_02；
- activate NAME：先备份当前源码，再把指定候选同步到源码目录；同步时排除 _ai_work、.git、缓存、虚拟环境和构建产物；
- promote NAME：将指定候选保存为 mvp；
- checkpoint NAME：保存当前源码检查点；
- restore NAME：恢复 source_backup、mvp 或指定检查点。

所有覆盖操作必须先校验源路径和目标路径；目标备份已存在时拒绝覆盖；替换源码前必须自动备份当前版本。请写好脚本、参数校验和使用说明，不修改题目源码。
```

运行 `init`。四个候选必须来自同一份原始备份，每个会话只打开一个候选工作区。

## 三 四路首轮 Rollout

### Prompt 2 文档驱动 Rollout

在 `branch_doc_01`、`branch_doc_02` 分别开启新会话，使用同一个 Prompt：

```text
阅读 README、SCHEMA、样例和全部现有源码，在当前架构内完成题目要求。

编码前先创建：

- _ai_work/SPEC.md：用 AC-001 等编号整理功能、输入输出、接口、边界、错误行为和验收方式；
- _ai_work/PLAN.md：把验收项拆成可实施步骤，标注优先级和状态。

开始修改代码时创建 _ai_work/EVOLUTION.md，持续记录关键决策、发现的问题、实际修改和验证状态。未实际运行的测试只能标记 NOT_RUN。

实现时保持现有入口、公开接口、函数签名和输出格式；优先复用原结构，只做必要修改，不引入无关依赖或重构，不硬编码样例和本地路径。重点核对空值、边界值、重复和乱序数据、并列规则、精度、异常路径、连续调用、确定性和性能限制。

补充必要的可执行自测文件。完成后重新对照 README 和 SPEC 检查覆盖情况，并汇报修改文件、已覆盖验收项、关键假设和剩余风险。
```

### Prompt 3 纯 Rollout

在 `branch_pure_01`、`branch_pure_02` 分别开启新会话，使用同一个 Prompt：

```text
阅读 README、SCHEMA、样例和全部现有源码，在当前架构内完成题目要求。本轮无需创建过程文档。

准确落实输入输出、公开接口、错误行为和评分约束；保持现有入口、函数签名、数据结构和输出格式兼容；复用已有模块，控制修改范围，不引入无关依赖和重构，不硬编码样例或本地路径。

实现时检查空输入、缺失值、非法值、最小和最大边界、重复值、并列与顺序、精度和取整、连续调用、共享状态、异常恢复、确定性以及大规模输入。补充必要的可执行自测文件，并重新对照 README 和样例检查完整调用链。

完成后汇报修改文件、需求覆盖、实现取舍、关键假设和重点风险。
```

## 四 选出 MVP

对 4 个候选执行完全相同的流程：

```text
激活候选 -> 运行公开样例 -> 提交隐藏评测 -> 记录通过数和分数
```

先看隐藏用例和功能分；同分时选择接口更合规、修改更小、依赖更少的版本。将胜出版本保存为 `mvp`，再建立 `mvp-v1` 检查点。后续只修改 MVP 的工作副本，不覆盖基线。

## 五 Critic 驱动问题发现

Critic 负责验收合同、边界测试和问题定位，不修改业务代码。Actor 不得修改 Critic 的测试区。

### Prompt 4 Critic Planner

```text
你是当前 MVP 的 Critic/Planner。阅读 README、源码以及 _ai_work 中的全部内容，不修改业务代码。

若文档不存在，先建立：

- SPEC.md：编号化验收合同；
- PLAN.md：按风险排序的检查计划；
- EVOLUTION.md：当前问题和只追加的演进历史。

你的核心职责是持续寻找隐藏用例可能击中的契约边界。每轮完成以下工作：

1. 从 README 提取或修正验收项，并映射到具体代码路径；
2. 检查代码中的隐含假设、未覆盖分支和与契约不一致之处；
3. 选择当前最高风险，只研究一个问题；
4. 在 _ai_work/tests/diagnostic 中编写能够区分根因的最小测试；
5. 已被真实结果确认的问题，转入 _ai_work/tests/regression；
6. 更新 PLAN 和 EVOLUTION。

按题目实际情况考虑：空值和缺失字段；非法类型；零、负数、最小值、最大值及临界值前后；重复、并列、乱序和稳定排序；精度、取整、编码、时间边界和输出格式；重复调用、状态残留、缓存、随机性和幂等性；最大规模和性能退化；部分失败、超时、重试、资源清理、路径或网络变化；多个边界同时出现的真实生产组合。还应利用确定性、单调性、置换不变性、往返一致性和输入不可变性设计关系测试。

每个测试必须输出唯一测试编号、关联 AC、输入、预期、实际和状态，确保失败能定位到具体用例。没有运行证据时使用 NOT_RUN 或 HYPOTHESIS，不得写成 PASS。

本轮向我汇报：问题编号、关联验收项、代码依据、测试文件、预期结果、影响、建议修复方向，以及我需要执行的命令或步骤。等待我反馈实际结果。
```

### Prompt 5 把诊断结果反馈给 Critic

```text
诊断结果如下：

问题编号：【ISSUE】
测试编号：【TEST ID】
输入：【输入】
预期：【预期】
实际：【实际】
完整报错或日志：【内容】

请根据真实结果更新 PLAN、EVOLUTION 和测试状态，判断问题是 CONFIRMED、REJECTED 还是仍需诊断。

若已确认，请说明根因、影响范围、建议 Actor 采用的最小方案和修复后必须回归的测试；若证据不足，请继续编写能够区分不同根因的最小诊断用例。不要修改业务代码。
```

## 六 Actor 分析和修复

每个问题都先分析、人工确认，再修改代码。

### Prompt 6 Actor 分析方案

```text
阅读 README、当前源码、SPEC.md、PLAN.md、EVOLUTION.md 和相关测试。

本轮问题：【ISSUE】
失败证据：【输入、预期、实际、报错】
Critic 建议：【建议】

先检查 Critic 的结论，不修改任何文件。向我汇报：

1. 实际根因和代码路径；
2. 问题是否成立；
3. 最小修复方案；
4. 对现有接口和已通过行为的影响；
5. 必须执行的回归测试；
6. 修改风险。

等待我确认方案。
```

### Prompt 7 Actor 实施修复

```text
按已确认方案修复：【ISSUE】

确认方案：【方案】

修改前重新读取相关文件的磁盘最新版本。只修改必要的业务代码，保持现有入口、接口和已通过行为；不修改 SPEC、PLAN、EVOLUTION 和 _ai_work/tests，也不进行无关重构。

完成后汇报实际根因、修改文件、关键改动、可能影响的旧行为，以及需要执行的目标测试和完整回归测试。
```

## 七 评测和继续迭代

Actor 修改前先建立检查点。修改后由我依次运行：

```text
目标诊断测试 -> 全部回归测试 -> 公开样例 -> 隐藏评测
```

### Prompt 8 把完整结果反馈给 Critic

```text
本轮结果：

轮次：【ROUND】
问题：【ISSUE】
目标测试：【结果】
完整回归：【结果】
公开样例：【结果】
隐藏用例：【通过数/总数】
原分数：【分数】
新分数：【分数】
异常信息：【内容】

请更新 PLAN、EVOLUTION 和测试状态，并判断：

- KEEP：问题已解决、没有回归，隐藏分数不下降；
- REJECT：出现回归或隐藏分数下降，应恢复检查点；
- PENDING：证据不足或结果不稳定，需要继续诊断。

汇报本轮结论、剩余最高风险，以及下一轮最值得验证的一个问题。不要修改业务代码。
```

然后重复：

```text
Critic 找一个高风险问题
-> 我运行诊断并反馈
-> Actor 分析方案
-> 我确认
-> Actor 修改
-> 我运行回归和隐藏评测
-> Critic 记录结果并决定保留或回退
```

隐藏用例达到 100% 后停止修改。未达到 100% 时，如果连续迭代没有提升、缺少可靠证据或提交机会不足，也应停止猜测并保留当前最高分版本。
